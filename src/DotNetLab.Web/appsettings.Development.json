{
  // Se fusiona sobre appsettings.json cuando ASPNETCORE_ENVIRONMENT=Development.
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      // Muestra las peticiones que atraviesa el proxy hacia la API.
      "System.Net.Http.HttpClient": "Information"
    }
  }
}
